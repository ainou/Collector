// Dev-only: set to true as a fallback for screen recorders that do not
// capture the backdrop blur correctly. When true, both the capture and
// reader windows render a static gradient instead of relying on the
// system backdrop blur.
export const ENABLE_DEMO_FAKE_BG = false;
